# Allocate host-specific numeric IDs. The matching group becomes the user's
# primary group; neither the unit nor this file relies on a fixed UID/GID.
g recasaos-public -
u recasaos-public - "ReCasaOS public-file portal" /nonexistent /usr/sbin/nologin
