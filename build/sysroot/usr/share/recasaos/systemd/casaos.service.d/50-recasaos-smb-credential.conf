# This drop-in is staged under /usr/share and is not active by default.
# Activate it only after the fixed source keyring has been durably provisioned
# and audited. A missing source is intentionally a hard systemd start failure.
[Service]
LoadCredential=recasaos-smb-keyring:/etc/recasaos/recasaos-smb-keyring
