{
  "name": "@recasaos/public-files-browser-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.18.0"
  },
  "scripts": {
    "smoke": "playwright test --config=playwright.config.js",
    "test:diagnostics": "node --test unit/*.test.js"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
